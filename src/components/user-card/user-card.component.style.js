import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  cardImage: {
    height: '400px',
  },
  cardBody: {},
  card: {
    height: '100%',
  },
}));
